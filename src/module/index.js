import React from 'react';

const ModuleAsync = () => (
  <div>
    I am a lazy component that is imported using a relative path to a directory
    which has an index.js file
  </div>
);

export default ModuleAsync;
